const __vite__mapDeps=(i,m=__vite__mapDeps,d=(m.f||(m.f=["_astro/CaseStudyOverlay.CJfxtRBE.js","_astro/jsx-runtime.D_zvdyIk.js","_astro/index.DiEladB3.js","_astro/index.tUWw1UFN.js","_astro/CaseStudyContent.CzTbRikh.js","_astro/preload-helper.BlTxHScW.js","_astro/index.Pl_XcmgS.js","_astro/index.UCiZe19v.js"])))=>i.map(i=>d[i]);
import{_ as I}from"./preload-helper.BlTxHScW.js";import{j as e}from"./jsx-runtime.D_zvdyIk.js";import{r as i}from"./index.DiEladB3.js";import{r as H}from"./index.Pl_XcmgS.js";import"./index.UCiZe19v.js";const L=()=>I(()=>import("./CaseStudyOverlay.CJfxtRBE.js"),__vite__mapDeps([0,1,2,3,4,5,6,7])),B=i.lazy(L);let E=!1;const $=()=>{E||(E=!0,L())},j=[{slug:"garcia",index:"01",name:"Garcia HandPicked",category:"Packaging · Flower / Edibles / Pre-rolls",position:"Jerry Garcia, officially licensed. The packaging plays like a sixties poster.",blurb:"Grateful Dead concert-poster energy meets compliant cannabis packaging. Fluid rainbow liquid-marble artwork and an integrated Jerry Garcia portrait carry a deep SKU range and strain-coded variants.",hero:"/portfolio/garcia/garcia-brand-cover-1x1.webp",heroW:1080,heroH:1080,heroPos:"50% 20%",effect:"still",square:!0,caseStudy:!0,bg:"#080608",accent:"#b6ff2e",accentText:"#0a0a0a",glow:"rgba(182,255,46,0.30)"},{slug:"dodrops",index:"02",name:"DO Drops",logo:{src:"/brand-logo/dodrops.png",w:200,h:200,em:1.1},category:"Branding · Gummy Pouches & Retail Display",position:"Designed around how you want to feel.",blurb:["DO DROPS makes choosing the right gummy simple. Each formula has its own bold pouch color, making Focus, Sleep, Relax, Relief, Energy, and Mild easy to recognize at a glance.","Soft aura gradients carry that same playful look into campaign imagery, retail displays, and a Pride edition created with the Transgender Law Center, keeping DO DROPS bright, expressive, and easy to spot wherever it appears."],hero:"/portfolio/dodrops/dodrops-brand-cover-1x1.webp",heroW:900,heroH:900,effect:"still",square:!0,linksOut:!0,bg:"#0b0714",accent:"#ff0d6c",accentText:"#0a0a0a",glow:"rgba(255,13,108,0.30)"},{slug:"strane",index:"03",name:"Strane",logo:{src:"/brand-logo/strane.png",w:474,h:157,em:1},category:"Branding · Disposable Vapes & Cartridges",position:"Cannabis vapes with a punk streak: street-art grit meets clean device renders.",blurb:"A distinctly punk visual system: distressed brick, paint splatter and retro collage against hard acid-yellow-and-black, while premium 3D device renders keep it product-forward.",hero:"/portfolio/strane/strane-brand-cover-1x1.webp",heroW:1080,heroH:1080,heroPos:"24% 15%",effect:"still",square:!0,caseStudy:!0,bg:"#0b0b06",accent:"#e5ff00",accentText:"#0a0a0a",glow:"rgba(229,255,0,0.30)"},{slug:"avitas",index:"04",name:"AVITAS",logo:{src:"/brand-logo/avitas-wordmark.png",w:854,h:244,em:1},category:"Branding · Vape Cartridges & Gummies",position:"Illustrated landscapes carry the identity, one scene per tier.",blurb:"A full identity system where each tier owns an illustrated landscape: mountains, ocean and botanicals in turquoise-to-navy with gold accents, anchored by a distinctive geometric “A” mark.",hero:"/portfolio/avitas/avitas-brand-cover-1x1.webp",heroW:1080,heroH:1080,heroPos:"50% 84%",effect:"still",square:!0,caseStudy:!0,bg:"#04141c",accent:"#e8c87a",accentText:"#0a0a0a",glow:"rgba(64,200,210,0.30)"},{slug:"hellavated",index:"05",name:"Hellavated",logo:{src:"/brand-logo/hellavated.png",w:3945,h:871,em:1},category:"Packaging · Edibles, Devices & Beverages",position:"Gen-Z edibles branding, volume all the way up.",blurb:"Fruit-illustration hero panels meet a black technical compliance side, a two-tone system spanning 18+ gummy flavors, CloudBar vape devices and the Baja beverage line.",hero:"/portfolio/hellavated/hellavated-brand-cover-1x1.webp",heroW:1080,heroH:1080,heroPos:"50% 84%",effect:"still",square:!0,caseStudy:!0,bg:"#070707",accent:"#ff4fd8",accentText:"#0a0a0a",glow:"rgba(255,120,220,0.35)"},{slug:"lootbar",index:"06",name:"Lootbar",logo:{src:"/brand-logo/loot-bar.png",w:302,h:86,em:.85},category:"Packaging · Disposable Vapes & Key Art",position:"Vaporwave flavor system for LightHouse, where every flavor is a loot drop.",blurb:"Every flavor staged as an open treasure chest with the device floating beside it as the prize, inside one neon HUD frame that recolors across 11 key-arts. Y2K chrome lettering, pixel hearts and arcade checkerboards run throughout.",hero:"/portfolio/lootbar/hi/lootbar-blueberry-dream.webp",heroW:1200,heroH:1200,effect:"crt",square:!0,caseStudy:!0,bg:"#02040e",accent:"#00e0f0",accentText:"#0a0a0a",glow:"rgba(0,224,240,0.30)"},{slug:"better",index:"07",name:"BETTER",logo:{src:"/brand-logo/better-hero.png",w:1688,h:288,em:1},category:"Packaging · Cartridges & Gummies",position:"An occasion-based cannabis system that colors by time of day: Days, Sleep, Anytime.",blurb:"Three occasions, three worlds: sunset orange for Sunshine Tangerine days, midnight navy for Midnight Mixed Berry sleep, lavender-and-lemon for Easy Lemon Lavender anytime. Script sub-wordmarks sit under one bold BETTER sans, from blister-carded cartridges to gummy pouches.",hero:"/portfolio/better/hi/better-pink-picasso-hero-1x1.webp",heroW:1898,heroH:1898,effect:"still",square:!0,caseStudy:!0,bg:"#15060f",accent:"#ff4fa3",accentText:"#0a0a0a",glow:"rgba(255,79,163,0.32)"},{slug:"zeife",index:"08",name:"Zëife",category:"Branding · Soaps & Aromatics",position:"A crescent-moon-and-lotus identity for a soap & aromatics house, the studio’s first outside cannabis.",blurb:"One pink mark, a crescent moon cradling a lotus, set in Pantone-specced moon-dust gray and lotus pink: debossed into black card stock, screened onto shopping bags, framed art-nouveau on business cards. Identity first; the product line comes next.",hero:"/portfolio/zeife/zeife-logo-debossed-1x1.webp",heroW:1080,heroH:1080,effect:"still",square:!0,caseStudy:!1,bg:"#0d090b",accent:"#e9a7b8",accentText:"#0a0a0a",glow:"rgba(233,167,184,0.26)"},{slug:"dougie",index:"09",name:"Dougie",logo:{src:"/brand-logo/dougie.png",w:881,h:227,em:1},category:"Packaging · Infused Blunts & POS",position:"Graffiti-wall packaging for cannabis-infused blunts, distributed under LightHouse.",blurb:"One gold wordmark over a scratched-up graffiti wall that recolors by strain: green for hybrid, purple for indica, orange for sativa, across one-gram and two-gram pouches. A ripped-paper table tent carries the same wall onto the dispensary counter under the line “Make More Mischief.”",hero:"/portfolio/dougie/dougie-strain-trio-trim.webp",heroW:1142,heroH:1159,effect:"float",square:!0,bloom:"rgba(255,202,79,0.15)",caseStudy:!1,bg:"#0a0c06",accent:"#ffca4f",accentText:"#0a0a0a",glow:"rgba(156,215,100,0.28)"},{slug:"ardent",index:"10",name:"Ardent",category:"Packaging · Infusion & Edibles Kits",position:"A kit line where every recipe gets its own color, pattern and hand-painted plate.",blurb:"Each corrugated mailer tiles the lowercase swirl monogram with the recipe’s own motif: cocoa pods for Mini Truffle Cups, apples for Apple Pie. A textured paper label fronts the box with a watercolor plate of the finished bake, and the wordmark repeats inside the lid. Shown across Apple Pie, Olive Oil, Birthday Cupcakes, Breakfast Cloud Cakes and Mini Truffle Cups.",hero:"/portfolio/ardent/ardent-truffle-cups-kit.webp",heroW:1400,heroH:1400,effect:"still",square:!0,caseStudy:!1,bg:"#120c09",accent:"#7ea6e0",accentText:"#0a0a0a",glow:"rgba(126,166,224,0.24)"}],v="#FF5A1F",F=(t,g)=>({src:t.hero,width:t.heroW,height:t.heroH,decoding:"async",draggable:!1,...g?{fetchPriority:"high"}:{loading:"lazy"}});function G({p:t,priority:g}){const l=F(t,g);return t.effect==="float"?e.jsx("div",{className:"rw-float",style:t.bloom?{"--rw-bloom":t.bloom}:void 0,children:e.jsx("img",{...l,alt:`${t.name} device`,className:"rw-float-device"})}):t.effect==="brick"?e.jsxs("div",{className:"rw-brick",children:[t.brick&&e.jsx("div",{className:"rw-brick-bg",style:{backgroundImage:`url(${t.brick})`},"aria-hidden":"true"}),e.jsx("div",{className:"rw-brick-splatter","aria-hidden":"true"}),e.jsx("img",{...l,alt:`${t.name} device`,className:"rw-brick-device"})]}):t.effect==="crt"?e.jsxs("div",{className:"rw-crt",children:[e.jsx("img",{...l,alt:`${t.name} key visual`,className:"rw-crt-art"}),e.jsx("div",{className:"rw-crt-ghost rw-crt-ghost-r","aria-hidden":"true",children:e.jsx("img",{...l,alt:"",loading:"lazy"})}),e.jsx("div",{className:"rw-crt-ghost rw-crt-ghost-c","aria-hidden":"true",children:e.jsx("img",{...l,alt:"",loading:"lazy"})}),e.jsx("div",{className:"rw-crt-lines","aria-hidden":"true"}),e.jsx("div",{className:"rw-crt-vig","aria-hidden":"true"})]}):t.effect==="still"?e.jsxs("div",{className:"rw-still",children:[e.jsx("img",{...l,alt:`${t.name} key visual`,className:t.heroPos?"rw-still-art rw-still-art-tight":"rw-still-art",style:t.heroPos?{objectPosition:t.heroPos}:void 0}),e.jsx("div",{className:"rw-still-vig","aria-hidden":"true"})]}):t.effect==="landscape"?e.jsxs("div",{className:"rw-land",children:[t.back&&e.jsx("div",{className:"rw-land-bg",style:{backgroundImage:`url(${t.back})`},"aria-hidden":"true"}),e.jsx("div",{className:"rw-land-vig","aria-hidden":"true"}),e.jsx("img",{...l,alt:`${t.name} landscape`,className:"rw-land-img"})]}):e.jsxs("div",{className:"rw-marble-wrap",children:[e.jsx("div",{className:"rw-marble","aria-hidden":"true",children:e.jsx("div",{className:"rw-marble-flow"})}),e.jsx("img",{...l,alt:`${t.name} hero`,className:"rw-marble-img"})]})}function V({p:t,priority:g,onOpen:l}){const w=!!(t.caseStudy||t.linksOut),f=t.caseStudy?l:void 0;return e.jsxs("article",{className:t.square?"rw-panel rw-panel-square":"rw-panel",onPointerOver:t.caseStudy?$:void 0,style:{background:`radial-gradient(120% 120% at 100% 0%, ${t.glow} 0%, transparent 45%), ${t.bg}`,borderColor:"rgba(255,255,255,0.08)"},children:[e.jsxs("div",{className:"rw-panel-visual",children:[e.jsx(G,{p:t,priority:g}),e.jsx("span",{className:"rw-panel-index",style:{color:t.accent},children:t.index})]}),e.jsxs("div",{className:"rw-panel-copy",children:[e.jsx("span",{className:"rw-panel-cat",style:{color:t.accent,borderColor:"rgba(255,255,255,0.14)"},children:t.category}),e.jsx("h3",{className:"rw-panel-name",children:t.logo?e.jsx("img",{className:"rw-panel-logo",src:t.logo.src,width:t.logo.w,height:t.logo.h,style:{height:`${t.logo.em}em`},alt:t.name,loading:"lazy",decoding:"async"}):t.name}),e.jsx("p",{className:"rw-panel-pos",children:t.position}),(Array.isArray(t.blurb)?t.blurb:[t.blurb]).map((m,T)=>e.jsx("p",{className:"rw-panel-blurb",children:m},T)),w?e.jsxs("a",{href:`/work/${t.slug}`,"data-project":t.slug,className:"rw-panel-cta",style:{color:t.accent},onClick:f?m=>f(t.slug,m):void 0,children:[e.jsx("span",{children:"View Case Study"}),e.jsx("svg",{width:"14",height:"14",viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2.5",strokeLinecap:"round",strokeLinejoin:"round",children:e.jsx("path",{d:"M7 17L17 7M17 7H7M17 7V17"})})]}):e.jsx("span",{className:"rw-panel-cta rw-panel-cta-soon","aria-disabled":"true",children:e.jsx("span",{children:"Case Study Soon"})})]}),w&&e.jsx("a",{href:`/work/${t.slug}`,className:"rw-panel-hit",tabIndex:-1,"aria-hidden":"true",onClick:f?m=>f(t.slug,m):void 0})]})}function Z(){const t=i.useRef(null),g=i.useRef(null),l=i.useRef(1),w=i.useRef(()=>{}),[f,m]=i.useState(1),[T,C]=i.useState(!0),[A,P]=i.useState(!1),[N,y]=i.useState(null),x=i.useRef(!1),q=i.useCallback((r,a)=>{a.defaultPrevented||a.metaKey||a.ctrlKey||a.shiftKey||a.altKey||a.button!==0||(a.preventDefault(),history.pushState({csOverlay:!0},"",`/work/${r}`),x.current=!0,y(r))},[]);i.useEffect(()=>{const r=()=>{const a=window.location.pathname.match(/^\/work\/([\w-]+)\/?$/),o=a&&j.find(n=>n.slug===a[1]&&n.caseStudy);o?(x.current=!0,y(o.slug)):(x.current=!1,y(null))};return window.addEventListener("popstate",r),()=>window.removeEventListener("popstate",r)},[]);const R=i.useCallback(()=>{x.current?history.back():y(null)},[]),M=i.useCallback(r=>{history.replaceState({csOverlay:!0},"",`/work/${r}`),y(r)},[]),O=r=>{const a=r.querySelector(".rw-strip-item");if(!a)return r.clientWidth||1;const o=parseFloat(getComputedStyle(r).columnGap||"0")||0;return a.getBoundingClientRect().width+o};i.useEffect(()=>{const r=t.current;if(!r)return;let a=-1,o=null,n=null;const h=()=>{l.current=O(r)||1},p=()=>{const c=r.scrollWidth-r.clientWidth,s=r.scrollLeft;if(g.current){const D=c>0?s/c:0;g.current.style.transform=`scaleX(${Math.min(1,Math.max(0,D))})`}const b=Math.min(j.length,Math.round(s/l.current)+1);b!==a&&(a=b,m(b));const S=s<=1;S!==o&&(o=S,C(S));const z=s>=c-1;z!==n&&(n=z,P(z))};h(),p(),r.addEventListener("scroll",p,{passive:!0});const u=()=>{h(),p()};return window.addEventListener("resize",u),r.querySelectorAll("img").forEach(c=>{c.complete||c.addEventListener("load",u,{once:!0})}),()=>{r.removeEventListener("scroll",p),window.removeEventListener("resize",u)}},[]),i.useEffect(()=>{const r=t.current;if(!r)return;const a=Array.from(r.querySelectorAll(".rw-strip-item"));if(!a.length)return;const o=new IntersectionObserver(n=>{for(const h of n)h.target.classList.toggle("rw-idle",!h.isIntersecting)},{rootMargin:"400px 400px"});return a.forEach(n=>o.observe(n)),()=>o.disconnect()},[]),i.useEffect(()=>{const r=t.current;if(!r)return;let a=!1,o=!1,n=0,h=0;const p=s=>{s.pointerType==="touch"||s.button!==0||(w.current(),a=!0,o=!1,n=s.clientX,h=r.scrollLeft)},u=s=>{if(!a)return;const b=s.clientX-n;!o&&Math.abs(b)>4&&(o=!0,r.classList.add("rw-dragging"),r.setPointerCapture?.(s.pointerId),window.getSelection()?.removeAllRanges()),o&&(r.scrollLeft=h-b,s.preventDefault())},d=()=>{a=!1,r.classList.remove("rw-dragging")},c=s=>{o&&(s.preventDefault(),s.stopPropagation(),o=!1)};return r.addEventListener("pointerdown",p),r.addEventListener("pointermove",u),window.addEventListener("pointerup",d),window.addEventListener("pointercancel",d),r.addEventListener("click",c,!0),()=>{r.removeEventListener("pointerdown",p),r.removeEventListener("pointermove",u),window.removeEventListener("pointerup",d),window.removeEventListener("pointercancel",d),r.removeEventListener("click",c,!0)}},[]),i.useEffect(()=>{const r=t.current;if(!r)return;const a=window.matchMedia("(prefers-reduced-motion: reduce)").matches;let o=r.scrollLeft,n=0;const h=()=>{n&&cancelAnimationFrame(n),n=0,o=r.scrollLeft};w.current=h;const p=()=>{const d=r.scrollWidth-r.clientWidth;o=Math.max(0,Math.min(d,o));const c=r.scrollLeft,s=o-c;if(Math.abs(s)<1){r.scrollLeft=o,n=0;return}if(r.scrollLeft=c+s*.18,r.scrollLeft===c){n=0;return}n=requestAnimationFrame(p)},u=d=>{if(!(Math.abs(d.deltaX)<=Math.abs(d.deltaY))){if(d.preventDefault(),d.stopPropagation(),a){r.scrollLeft+=d.deltaX;return}n||(o=r.scrollLeft),o+=d.deltaX,n||(n=requestAnimationFrame(p))}};return r.addEventListener("wheel",u,{passive:!1}),()=>{r.removeEventListener("wheel",u),n&&cancelAnimationFrame(n),w.current=()=>{}}},[]);const k=r=>{const a=t.current;a&&(w.current(),a.scrollBy({left:r*l.current,behavior:"smooth"}))},W=r=>{r.key==="ArrowRight"?(r.preventDefault(),k(1)):r.key==="ArrowLeft"&&(r.preventDefault(),k(-1))};return e.jsxs("section",{id:"work",className:"rw-section relative z-20",style:{background:"#050505"},children:[e.jsxs("div",{className:"rw-wrap",children:[e.jsxs("div",{className:"rw-head",children:[e.jsxs("p",{className:"rw-eyebrow",children:[e.jsx("span",{className:"rw-eyebrow-bracket","aria-hidden":"true",children:"[  "}),"Recent ",e.jsx("span",{className:"rw-eyebrow-accent",children:"Work"}),e.jsx("span",{className:"rw-eyebrow-bracket","aria-hidden":"true",children:"  ]"})]}),e.jsxs("h2",{className:"rw-title",children:["Brands, with ",e.jsx("span",{style:{color:v},children:"conviction"}),"."]}),e.jsx("p",{className:"rw-intro",children:"Each its own world, built end to end."})]}),e.jsx("div",{className:"w-full overflow-hidden",children:e.jsx("div",{ref:t,className:"rw-strip",role:"group","aria-roledescription":"carousel","aria-label":"Recent work",tabIndex:0,onKeyDown:W,children:j.map((r,a)=>e.jsx("div",{className:"rw-strip-item",children:e.jsx(V,{p:r,priority:a===0,onOpen:q})},r.slug))})}),e.jsxs("div",{className:"rw-foot",children:[e.jsxs("div",{className:"rw-count",children:[e.jsx("span",{style:{color:v},children:String(f).padStart(2,"0")}),e.jsx("span",{className:"rw-count-sep",children:"/"}),e.jsx("span",{children:String(j.length).padStart(2,"0")})]}),e.jsx("div",{className:"rw-progress-track",children:e.jsx("div",{ref:g,className:"rw-progress-bar",style:{background:`linear-gradient(90deg, ${v}, #FFA040)`}})}),e.jsxs("div",{className:"rw-arrows",children:[e.jsx("button",{type:"button",className:"rw-arrow","aria-label":"Previous project",onClick:()=>k(-1),disabled:T,children:e.jsx("svg",{width:"18",height:"18",viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2.5",strokeLinecap:"round",strokeLinejoin:"round",children:e.jsx("path",{d:"M15 18l-6-6 6-6"})})}),e.jsx("button",{type:"button",className:"rw-arrow","aria-label":"Next project",onClick:()=>k(1),disabled:A,children:e.jsx("svg",{width:"18",height:"18",viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2.5",strokeLinecap:"round",strokeLinejoin:"round",children:e.jsx("path",{d:"M9 6l6 6-6 6"})})})]})]})]}),N&&H.createPortal(e.jsx(i.Suspense,{fallback:null,children:e.jsx(B,{slug:N,onClose:R,onNext:M})}),document.body),e.jsx("style",{children:`
        .rw-section { color: #fff; }
        .rw-wrap { padding: 3.5rem 0 3rem; }

        /* ── Header ── */
        /* Head and foot sit on the shared page grid (--zg-content /
           --zg-gutter in global.css) so the eyebrow and the 01/09 counter
           start on the same line as every other section heading. The strip
           itself stays full-bleed on purpose: its inline padding centres the
           active card, which is the whole point of the carousel. */
        .rw-head {
          width: 100%;
          max-width: var(--zg-content);
          margin-inline: auto;
          padding: 0 var(--zg-gutter) 1.75rem;
        }
        /* Same eyebrow token as every other section (see global.css). This
           was the one section naming itself in its own dialect (no brackets,
           0.35em, a dimmer white), which is half of the unsynced-system note.
           The brackets are decoration, so they are aria-hidden, and they come
           off on phones where this element is promoted to the heading. */
        .rw-eyebrow {
          font-family: 'Montserrat', sans-serif;
          font-size: var(--zg-eyebrow-size);
          font-weight: var(--zg-eyebrow-weight);
          letter-spacing: var(--zg-eyebrow-track);
          text-transform: uppercase;
          color: var(--zg-eyebrow-ink);
          margin-bottom: 0.9rem;
        }
        .rw-eyebrow-accent { color: inherit; }
        .rw-title {
          font-family: 'Montserrat', sans-serif;
          font-weight: 900;
          text-transform: uppercase;
          line-height: 0.95;
          letter-spacing: -0.01em;
          font-size: clamp(2rem, 4.5vw, 4rem);
          color: #fff;
        }
        .rw-intro {
          margin-top: 0.9rem;
          max-width: 34rem;
          color: rgba(255,255,255,0.42);
          font-size: 0.95rem;
          line-height: 1.5;
        }

        /* ── Native horizontal-scroll strip ── */
        .rw-strip {
          /* Tim: "make the cards shorter in horizontal length because the
             other cards have too much whitespace."

             The cap, not the vw term, is what had to move: at 1440 the card
             is already 72vw (1037px) and has no slack at all, so the dead
             width only ever showed up on wide screens where the 1280 cap was
             binding. Measured at 1800: card 1280, copy column 720, but the
             text caps at 27rem/432px, so ~190px of that column was empty.

             1120 is the floor that removes the slack WITHOUT reflowing a
             single line. The copy has to keep at least 432px of text width
             after its 2x3rem padding, which needs card >= 1086 on the square
             panels (their visual half is a fixed square of --rw-card-h) and
             card >= 1112 on the 1.05fr/0.95fr ones. 1120 clears both. That
             matters more than it sounds: Garcia's copy already measures 558px
             inside a 560px panel, so any narrowing that rewrapped the text
             would push it straight out of the card. */
          --rw-card: min(72vw, 1120px);
          /* Named because .rw-panel-square has to reason about it: a square
             visual half is exactly as wide as the panel is tall. */
          --rw-card-h: clamp(420px, 60vh, 560px);
          display: flex;
          gap: 2rem;
          overflow-x: auto;
          overflow-y: hidden;
          overscroll-behavior-x: contain;
          -webkit-overflow-scrolling: touch;
          scrollbar-width: none;
          /* The strip is full-bleed and it opens like one: card 01 sits a
             hair off the viewport edge, not on the page's text column. It was
             centred in the viewport before (~200px in at 1440, 320 at 1920),
             which hid most of card 02 and made the strip read as one card
             rather than a run of them. 4vw, capped, is the inset picked off
             the reference: ~77px at 1920, where card 02 then shows ~530px.
             Deliberately NOT --zg-gutter or the content column: this section
             opts out of the page grid the way the client marquee does, and
             the eyebrow/heading/progress bar above and below still hold that
             line. Symmetric, so the run-out on the right matches. */
          padding-block: 0.5rem 1.5rem;
          padding-inline: clamp(24px, 4vw, 80px);
          cursor: grab;
        }
        .rw-strip::-webkit-scrollbar { display: none; }
        .rw-strip:focus-visible {
          outline: 2px solid ${v};
          outline-offset: 4px;
        }
        .rw-strip.rw-dragging {
          cursor: grabbing;
          scroll-behavior: auto;
          user-select: none;
          -webkit-user-select: none;
        }
        .rw-strip.rw-dragging * {
          user-select: none;
          -webkit-user-select: none;
        }
        .rw-strip.rw-dragging .rw-panel { pointer-events: none; }
        .rw-strip-item {
          flex: 0 0 auto;
          width: var(--rw-card);
          height: var(--rw-card-h);
        }

        /* ── Footer: counter + progress + arrows ── */
        .rw-foot {
          display: flex;
          align-items: center;
          gap: 1.5rem;
          width: 100%;
          max-width: var(--zg-content);
          margin-inline: auto;
          padding: 1rem var(--zg-gutter) 0;
        }
        .rw-count {
          font-family: 'Montserrat', sans-serif;
          font-weight: 800;
          font-size: 0.9rem;
          letter-spacing: 0.1em;
          color: rgba(255,255,255,0.35);
          display: flex;
          gap: 0.35rem;
        }
        .rw-count-sep { color: rgba(255,255,255,0.2); }
        .rw-progress-track {
          flex: 1 1 auto;
          height: 3px;
          border-radius: 3px;
          background: rgba(255,255,255,0.1);
          overflow: hidden;
        }
        .rw-progress-bar {
          height: 100%;
          width: 100%;
          transform: scaleX(0);
          transform-origin: left center;
          border-radius: 3px;
          transition: transform 0.1s linear;
        }
        .rw-arrows { display: flex; gap: 0.6rem; flex: none; }
        .rw-arrow {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          width: 2.75rem;
          height: 2.75rem;
          border-radius: 999px;
          border: 1px solid rgba(255,255,255,0.16);
          background: rgba(255,255,255,0.03);
          color: #fff;
          cursor: pointer;
          transition: background 0.2s ease, border-color 0.2s ease, opacity 0.2s ease, transform 0.1s ease;
        }
        .rw-arrow:hover:not(:disabled) {
          background: rgba(255,255,255,0.09);
          border-color: rgba(255,255,255,0.32);
        }
        .rw-arrow:active:not(:disabled) { transform: scale(0.94); }
        .rw-arrow:disabled { opacity: 0.28; cursor: default; }

        /* ── Panel ── */
        .rw-panel {
          position: relative;
          width: 100%;
          height: 100%;
          border: 1px solid rgba(255,255,255,0.08);
          border-radius: 1.75rem;
          overflow: hidden;
          display: grid;
          grid-template-columns: 1.05fr 0.95fr;
          box-shadow: 0 30px 90px rgba(0,0,0,0.55);
        }
        /* Square masters (Quynh's 1:1 promo comps) get a square hole instead of
           the default 1.05fr one, so object-fit: cover has nothing to trim and
           the whole comp survives. The width is the panel's inner height:
           --rw-card-h less the 1px borders, which count because Tailwind's
           preflight puts everything in border-box. The copy half takes the rest,
           so it ends up WIDER here than on the other panels, not narrower. */
        .rw-panel-square {
          grid-template-columns: calc(var(--rw-card-h) - 2px) 1fr;
        }
        .rw-panel-visual {
          position: relative;
          overflow: hidden;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 2.5rem;
        }
        .rw-panel-index {
          position: absolute;
          top: 1.5rem;
          left: 1.75rem;
          font-family: 'Montserrat', sans-serif;
          font-weight: 900;
          font-size: 2.75rem;
          line-height: 1;
          opacity: 0.9;
          z-index: 5;
          text-shadow: 0 2px 20px rgba(0,0,0,0.5);
        }
        .rw-panel-copy {
          position: relative;
          z-index: 4;
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          justify-content: center;
          gap: 1rem;
          padding: 3rem;
          background: linear-gradient(90deg, transparent, rgba(0,0,0,0.35) 30%);
        }
        .rw-panel-cat {
          font-family: 'Montserrat', sans-serif;
          font-size: 0.65rem;
          font-weight: 700;
          letter-spacing: 0.18em;
          text-transform: uppercase;
          padding: 0.4rem 0.8rem;
          border: 1px solid;
          border-radius: 999px;
        }
        .rw-panel-name {
          font-family: 'Montserrat', sans-serif;
          font-weight: 900;
          text-transform: uppercase;
          font-size: clamp(1.8rem, 3vw, 3rem);
          line-height: 0.98;
          color: #fff;
        }
        /* Height comes from the inline em value; width follows the intrinsic
           ratio. max-width keeps the widest marks (BETTER is nearly 6:1) from
           pushing past the copy column on a narrow card, and object-fit means
           that clamp letterboxes rather than distorts. */
        .rw-panel-logo {
          display: block;
          width: auto;
          max-width: 100%;
          object-fit: contain;
        }
        .rw-panel-pos {
          font-size: clamp(1rem, 1.4vw, 1.35rem);
          font-weight: 600;
          line-height: 1.3;
          color: rgba(255,255,255,0.9);
          max-width: 26rem;
        }
        .rw-panel-blurb {
          font-size: 0.92rem;
          line-height: 1.6;
          color: rgba(255,255,255,0.55);
          max-width: 27rem;
        }
        .rw-panel-cta {
          display: inline-flex;
          align-items: center;
          gap: 0.5rem;
          margin-top: 0.5rem;
          font-family: 'Montserrat', sans-serif;
          font-size: 0.72rem;
          font-weight: 700;
          letter-spacing: 0.16em;
          text-transform: uppercase;
          transition: gap 0.25s ease, opacity 0.25s ease;
        }
        .rw-panel-cta:hover { gap: 0.85rem; opacity: 0.85; }
        /* The whole card is the click target, so the CTA answers to a hover
           anywhere on the card: that arrow sliding out is the only signal
           the card is live. */
        .rw-panel:hover .rw-panel-cta:not(.rw-panel-cta-soon) {
          gap: 0.85rem;
          opacity: 0.85;
        }
        .rw-panel-hit {
          position: absolute;
          inset: 0;
          z-index: 6;
          /* inherit, not pointer: the strip's grab/grabbing cursor is the drag
             affordance and it stays the primary gesture here. */
          cursor: inherit;
        }
        .rw-panel-cta-soon {
          color: rgba(255,255,255,0.32);
          cursor: default;
        }
        .rw-panel-cta-soon:hover { gap: 0.5rem; opacity: 1; }

        /* Cutout stages (marble / brick / landscape / float) are absolute like
           .rw-crt and .rw-still, so their art ignores .rw-panel-visual's padding
           and reaches the panel edge. The subject then measures against the WHOLE
           half: a cutout at 100% here is as big as the full-bleed art on the
           Lootbar / BETTER / Ardent panels, which is the look those three get. */
        .rw-marble-wrap,
        .rw-brick,
        .rw-land,
        .rw-float {
          position: absolute;
          inset: 0;
          overflow: hidden;
          display: flex;
          align-items: center;
          justify-content: center;
        }

        /* ── Garcia: liquid-marble ──
           The turn/breathe half of the motion. The colour band that slides
           across it is .rw-marble-flow, a child, because the slide used to be
           an animated background-position, and background-position is not a
           compositor property, so every frame re-rasterised a 40px-blurred
           conic gradient on the main thread, for a panel that is off screen
           most of the time. */
        .rw-marble {
          position: absolute;
          inset: -30%;
          opacity: 0.55;
          animation: rw-marble-turn 18s ease-in-out infinite;
          will-change: transform;
        }
        /* Same pixels, expressed as a translate. background-size: 200% 200%
           painted a 2W x 2H gradient behind the W x H layer and background-
           position slid it from x=0 to x=-W; this layer IS 2W wide, shows the
           gradient's middle horizontal band (100% 200% at 0% 50%, still a
           2W x 2H image), and travels the same -W, which is -50% of its own
           width. The parent's rotate/scale then applies on top, exactly as it
           did when the gradient was the parent's own background. */
        .rw-marble-flow {
          position: absolute;
          top: 0;
          left: 0;
          width: 200%;
          height: 100%;
          background: conic-gradient(from 0deg,
            #ff0040, #ff8a00, #ffe600, #37ff5b, #00c2ff, #7a3bff, #ff2fd0, #ff0040);
          background-size: 100% 200%;
          background-position: 0% 50%;
          filter: blur(40px) saturate(1.3);
          animation: rw-marble-flow 18s ease-in-out infinite;
          will-change: transform;
        }
        .rw-marble-img {
          position: relative;
          z-index: 2;
          height: 94%;
          width: auto;
          max-width: 88%;
          object-fit: contain;
          filter: drop-shadow(0 20px 40px rgba(0,0,0,0.5));
        }
        @keyframes rw-marble-turn {
          0%   { transform: rotate(0deg) scale(1); }
          50%  { transform: rotate(8deg) scale(1.12); }
          100% { transform: rotate(0deg) scale(1); }
        }
        @keyframes rw-marble-flow {
          0%   { transform: translate3d(0, 0, 0); }
          50%  { transform: translate3d(-50%, 0, 0); }
          100% { transform: translate3d(0, 0, 0); }
        }

        /* ── Strane: brick backdrop + device ── */
        .rw-brick-bg {
          position: absolute;
          inset: 0;
          background-size: cover;
          background-position: center;
          opacity: 0.55;
          animation: rw-brick-drift 24s ease-in-out infinite alternate;
        }
        .rw-brick-splatter {
          position: absolute;
          inset: 0;
          background:
            radial-gradient(circle at 20% 30%, rgba(229,255,0,0.25) 0, transparent 12%),
            radial-gradient(circle at 78% 70%, rgba(229,255,0,0.18) 0, transparent 16%),
            radial-gradient(circle at 60% 15%, rgba(229,255,0,0.12) 0, transparent 9%);
          mix-blend-mode: screen;
        }
        /* Trimmed render, so 100% height IS the shelf display: it stands the
           full height of the half, like a product on a shelf, instead of hovering
           at 46% of the width. */
        .rw-brick-device {
          position: relative;
          z-index: 2;
          height: 100%;
          width: auto;
          max-width: 92%;
          object-fit: contain;
          filter: drop-shadow(0 24px 44px rgba(0,0,0,0.7));
        }
        /* This one keeps its background-position term. Unlike the marble, the
           distance it travels is 12% of (layer width − COVER width), and cover
           width depends on the panel's aspect ratio at the current viewport:
           there is no transform that equals it at every size, so re-expressing
           it would be an approximation, not a translation. Measured: the term
           resolves to exactly 0 at the common desktop sizes (the layer is wider
           than 773:800, so cover fits width and there is no horizontal slack),
           ~7px at 1024x768 and ~0.5px on a phone. It is a cheap repaint too
           (an unblurred bitmap, not the marble's blurred conic), and off-screen
           panels no longer run it at all. */
        @keyframes rw-brick-drift {
          from { background-position: 0% 50%; transform: scale(1.05); }
          to   { background-position: 12% 50%; transform: scale(1.12); }
        }

        /* ── AVITAS: ken-burns landscape ── */
        .rw-land {
          background:
            radial-gradient(120% 90% at 50% 10%, rgba(64,200,210,0.22), transparent 60%);
        }
        /* The ken-burns moved off the pack and onto the landscape behind it: a
           product render that slowly grows and slides reads as a glitch, an
           illustrated backdrop that does is the effect this panel is named for. */
        .rw-land-bg {
          position: absolute;
          inset: 0;
          background-size: cover;
          background-position: 50% 55%;
          opacity: 0.72;
          animation: rw-kenburns 26s ease-in-out infinite alternate;
        }
        /* Same edge fade the crt / still panels use, so the illustration darkens
           into the panel instead of ending on a hard line. */
        .rw-land-vig {
          position: absolute;
          inset: 0;
          pointer-events: none;
          background:
            radial-gradient(115% 105% at 45% 50%, transparent 45%, rgba(4,20,28,0.85) 100%),
            linear-gradient(90deg, transparent 66%, rgba(4,20,28,0.9) 100%);
        }
        .rw-land-img {
          position: relative;
          z-index: 2;
          height: 92%;
          width: auto;
          max-width: 88%;
          object-fit: contain;
          filter: drop-shadow(0 26px 46px rgba(0,0,0,0.6));
        }
        @keyframes rw-kenburns {
          from { transform: scale(1.04) translateY(0); }
          to   { transform: scale(1.14) translateY(-1.5%); }
        }

        /* ── Dougie: floating cutout render ── */
        .rw-float {
          background:
            radial-gradient(120% 90% at 50% 12%, var(--rw-bloom, rgba(182,255,46,0.16)), transparent 62%);
        }
        /* Trimmed render, so this is sized against the whole half and the trio
           runs the full height. The bob is translate-only: a scale on a
           100%-sized cutout would clip it against the panel edge at the top of
           the cycle. */
        .rw-float-device {
          position: relative;
          z-index: 2;
          max-height: 96%;
          max-width: 92%;
          object-fit: contain;
          filter: drop-shadow(0 22px 42px rgba(0,0,0,0.6));
          animation: rw-float-bob 9s ease-in-out infinite alternate;
        }
        @keyframes rw-float-bob {
          from { transform: translateY(0.7%); }
          to   { transform: translateY(-0.7%); }
        }

        /* ── Lootbar: full-bleed key art under a CRT treatment ── */
        /* Absolute, so the art ignores .rw-panel-visual's padding and bleeds
           to the panel edge: this hero is composed key art, not a cutout. */
        .rw-crt { position: absolute; inset: 0; overflow: hidden; }
        /* Fills the whole half, like every other panel. This used to be a
           72%-tall letterboxed band because the hero was a 16:9 campaign
           visual that a near-square cover crop destroyed; the hero is now a
           1200x1200 flavor key art, so it fills edge to edge with ~1% crop and
           there is no dead ground above or below it. */
        .rw-crt-art,
        .rw-crt-ghost img {
          position: absolute;
          inset: 0;
          width: 100%;
          height: 100%;
          object-fit: cover;
          object-position: 50% 50%;
          display: block;
        }
        /* Channel ghosts: the art multiplied down to one channel, screened
           back over the base. Idle at opacity 0: only the tick reveals them. */
        /* The ghost box tracks the art box exactly: its tint fills its own
           box, so any mismatch would flood the gap with flat colour. */
        .rw-crt-ghost {
          position: absolute;
          inset: 0;
          mix-blend-mode: screen;
          opacity: 0;
          pointer-events: none;
        }
        .rw-crt-ghost img { inset: 0; height: 100%; mix-blend-mode: multiply; }
        .rw-crt-ghost-r { background: #ff2e55; animation: rw-crt-tick-r 7.2s steps(1, end) infinite; }
        .rw-crt-ghost-c { background: #2ee6ff; animation: rw-crt-tick-c 7.2s steps(1, end) infinite; }
        /* ~120ms of split every 7.2s. Deliberately weak: at 0.75 opacity the
           art washed out completely. This is a fringe, not a takeover. */
        @keyframes rw-crt-tick-r {
          0%,     1.09%  { opacity: 0.34; transform: translate3d(-3px, 1px, 0); }
          1.1%,   1.68%  { opacity: 0.22; transform: translate3d(2px, -1px, 0); }
          1.69%, 100%    { opacity: 0; transform: none; }
        }
        @keyframes rw-crt-tick-c {
          0%,     1.09%  { opacity: 0.34; transform: translate3d(3px, -1px, 0); }
          1.1%,   1.68%  { opacity: 0.22; transform: translate3d(-2px, 1px, 0); }
          1.69%, 100%    { opacity: 0; transform: none; }
        }
        .rw-crt-lines {
          position: absolute;
          inset: 0;
          pointer-events: none;
          background: repeating-linear-gradient(
            0deg,
            rgba(0,0,0,0.30) 0px,
            rgba(0,0,0,0.30) 1px,
            transparent 1px,
            transparent 3px
          );
        }
        .rw-crt-vig {
          position: absolute;
          inset: 0;
          pointer-events: none;
          background:
            radial-gradient(115% 105% at 50% 45%, transparent 58%, rgba(2,4,14,0.62) 100%),
            linear-gradient(90deg, transparent 72%, rgba(2,4,14,0.55) 100%);
        }

        /* ── Hellavated / BETTER / Zeife: full-bleed opaque comp, slow ken-burns ── */
        /* Absolute like .rw-crt, so the art ignores .rw-panel-visual's padding
           and bleeds to the panel edge: these are composed photographs with
           baked backgrounds, not cutouts. */
        .rw-still { position: absolute; inset: 0; overflow: hidden; }
        .rw-still-art {
          position: absolute;
          inset: 0;
          width: 100%;
          height: 100%;
          object-fit: cover;
          object-position: 50% 50%;
          display: block;
          transform-origin: 50% 50%;
          animation: rw-still-drift 48s ease-in-out infinite alternate;
          will-change: transform;
        }
        /* Art with a wordmark near the crop edge (Hellavated's cover) gets a
           gentler drift: the default 1.09 zoom plus translate walks the logo
           straight off the bottom of the half. */
        .rw-still-art-tight { animation-name: rw-still-drift-tight; }
        @keyframes rw-still-drift-tight {
          from { transform: scale(1.01) translate3d(0, 0, 0); }
          to   { transform: scale(1.045) translate3d(-0.5%, 0, 0); }
        }
        @keyframes rw-still-drift {
          from { transform: scale(1.02) translate3d(0, 0, 0); }
          to   { transform: scale(1.09) translate3d(-1.2%, -1%, 0); }
        }
        .rw-still-vig {
          position: absolute;
          inset: 0;
          pointer-events: none;
          background:
            radial-gradient(115% 105% at 50% 45%, transparent 58%, rgba(0,0,0,0.55) 100%),
            linear-gradient(90deg, transparent 72%, rgba(0,0,0,0.5) 100%);
        }

        /* ── Short desktop windows: tighten the copy so it cannot be clipped ──
           The card's height is 60vh-driven (--rw-card-h) but a panel's copy
           has a fixed intrinsic height, so on a laptop window the longest
           card outgrows it and .rw-panel's overflow: hidden silently eats
           the last lines. Measured on the DO DROPS panel, whose two-paragraph
           client copy is the longest on the strip: it overran by 39px at
           1440x800, 15px at 1600x900 and 6px at 1440x900. It fits at
           1000px+ of viewport height, which is why this starts below that
           and nothing changes on a tall screen.

           Padding and gap carry the fix because they cost no words. Together
           with the slightly tighter blurb leading they free ~90px by 800px
           tall, which clears the worst case with room and holds down to
           ~700px. Below that the strip has bigger problems than this. */
        @media (min-width: 1024px) and (max-height: 999px) {
          .rw-panel-copy {
            padding: clamp(1.5rem, 2.6vh, 2.5rem);
            gap: clamp(0.55rem, 1vh, 0.9rem);
          }
          .rw-panel-blurb { line-height: 1.5; }
        }

        /* ── Tablet / mobile: stack the panel, shrink the strip ── */
        @media (max-width: 1023px) {
          .rw-wrap { padding: 3.5rem 0 3rem; }
          .rw-head { padding: 0 var(--zg-gutter) 1.5rem; }
          .rw-strip {
            --rw-card: min(84vw, 480px);
            gap: 1rem;
            padding-block: 0.5rem 1rem;
          }
          .rw-foot { padding: 1rem var(--zg-gutter) 0; }
          .rw-strip-item {
            height: auto;
            min-height: 74vh;
          }
          .rw-panel {
            height: 100%;
            grid-template-columns: 1fr;
            grid-template-rows: 1.1fr 0.9fr;
          }
          .rw-panel-visual { padding: 1.5rem; min-height: 40vh; }
          /* Stacked, so the square is driven off the row instead of the column:
             the art row sizes to content and the visual's own aspect-ratio
             resolves against the card's definite width. The 40vh floor has to go
             or it would stretch the square back into a portrait. */
          .rw-panel-square {
            grid-template-columns: 1fr;
            grid-template-rows: auto 1fr;
          }
          .rw-panel-square .rw-panel-visual {
            aspect-ratio: 1;
            min-height: 0;
          }
          /* Every strip item stretches to the tallest one, and a square is
             shorter than the 1.1fr of 74vh the other panels give their art, so
             the copy row inherits the slack. Centred copy would then float in
             the middle of the card with a dead band above it; top-aligned, the
             text sits right under the art and the slack collects at the foot. */
          .rw-panel-square .rw-panel-copy { justify-content: flex-start; }
          .rw-panel-copy {
            padding: 1.75rem;
            background: linear-gradient(180deg, transparent, rgba(0,0,0,0.35) 20%);
          }
          .rw-panel-index { font-size: 2rem; top: 1rem; left: 1.25rem; }
          /* Square key art covers the mobile half without losing the device,
             so it fills here too and the RGB-split tick stays in play. */
          /* Copy sits below the art here, so the still panel's edge fade
             turns vertical too. */
          .rw-still-vig {
            background:
              radial-gradient(120% 110% at 50% 45%, transparent 62%, rgba(0,0,0,0.5) 100%),
              linear-gradient(180deg, transparent 72%, rgba(0,0,0,0.5) 100%);
          }
          /* Copy sits below the art here, so the edge fade turns vertical. */
          .rw-crt-vig {
            background:
              radial-gradient(120% 110% at 50% 45%, transparent 62%, rgba(2,4,14,0.55) 100%),
              linear-gradient(180deg, transparent 72%, rgba(2,4,14,0.55) 100%);
          }
        }

        /* ── Phones: the whole card has to fit one screen ──
           It was 868px on a 390x844, taller than the viewport before the
           section's own head and foot. The art is only 327 of that (the square
           is the signature and doesn't shrink); the other 541 was copy, and
           because the strip is a stretch flex row the single longest blurb was
           setting the height of all ten cards.

           So the blurb goes (its job is duplicated by the positioning line, and
           the full version is in the case study), the positioning line clamps,
           and the padding/gaps come in. What survives is the name and that
           positioning line: the category pill and the CTA come off too, further
           down in this block: the CTA visually only, since it stays the panel's
           one real focusable link (.rw-panel-hit is aria-hidden/tabIndex -1).

           Scoped to 767px so tablet keeps the blurb and the 74vh floor: it has
           the room, and the complaint was phones. Nothing here is read by the
           carousel: measureStride, the counter, the progress bar and the arrow
           steps are all width-derived, and card width is unchanged. */
        @media (max-width: 767px) {
          .rw-strip-item { min-height: 0; }
          .rw-panel-visual { min-height: 0; }
          .rw-panel-copy {
            padding: 1.25rem 1.5rem 1.5rem;
            gap: 0.6rem;
            justify-content: flex-start;
          }
          .rw-panel-cat {
            font-size: 0.6rem;
            letter-spacing: 0.14em;
            padding: 0.3rem 0.7rem;
          }
          .rw-panel-name { font-size: 1.5rem; line-height: 1.02; }
          /* Three lines, not two. The copy column is ~279px, so two lines hold
             ~66 characters and every one of the positioning strings (72 to
             105 chars) ended in an ellipsis mid-phrase: "…that puts
             psychedelic…" on the first card a visitor sees. */
          .rw-panel-pos {
            font-size: 0.95rem;
            line-height: 1.35;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
          }
          .rw-panel-blurb { display: none; }

          /* Tag pill and "View case study" both come off the card (owner's
             call). The CTA is only hidden *visually*: it stays in the a11y
             tree and the tab order, because it is the panel's one real link
             (.rw-panel-hit is aria-hidden with tabIndex -1). Tradeoff worth
             knowing: with no visible CTA, the three unbuilt projects lose the
             "Case Study Soon" marker that told you the tap does nothing. */
          .rw-panel-cat { display: none; }
          .rw-panel-cta {
            position: absolute;
            width: 1px;
            height: 1px;
            margin: 0;
            overflow: hidden;
            clip-path: inset(50%);
            white-space: nowrap;
          }

          /* ── Phones: the eyebrow IS the heading ──
             Title and intro are cut and the arrows go with them, so "RECENT
             WORK" carries the section on its own: headline mass (Montserrat
             900, white) but a label's positive tracking, because it is a
             nameplate rather than a sentence. One line, left-aligned: the
             strip and footer are left-anchored, and the break from the
             centred hero above is what reads as the section boundary.

             The h2 is hidden visually rather than with display:none: it is
             the section's only heading, and dropping it out of the a11y tree
             would leave a heading-level skip straight to the panels' h3s.
             The arrows hide at the .rw-arrows wrapper, not the buttons: an
             empty flex item would still leave its 1.25rem gap behind. Nothing
             else reads them: atStart/atEnd have no other consumer, and
             ArrowLeft/ArrowRight stay live on the focused strip. */
          .rw-title {
            position: absolute;
            width: 1px;
            height: 1px;
            margin: 0;
            padding: 0;
            overflow: hidden;
            clip-path: inset(50%);
            white-space: nowrap;
          }
          .rw-intro { display: none; }
          /* Anchors now stop 80px short of a section top (BaseLayout), so this
             no longer has to carry the nav clearance itself: the 5.5rem that
             did was showing up as a dead band above the heading on every
             ordinary scroll past. */
          .rw-wrap { padding: 3rem 0 2.5rem; }
          .rw-head { padding: 0 var(--zg-gutter) 1.25rem; }
          .rw-eyebrow {
            font-size: clamp(28px, 8.8vw, 36px);
            font-weight: 900;
            letter-spacing: 0.06em;
            line-height: 1;
            color: #fff;
            margin-bottom: 0;
            white-space: nowrap;
          }
          .rw-eyebrow-accent { color: ${v}; }
          /* Promoted to the heading here, so it drops the label furniture. */
          .rw-eyebrow-bracket { display: none; }
          .rw-arrows { display: none; }
          .rw-foot { gap: 1.25rem; padding: 0.75rem var(--zg-gutter) 0; }
          .rw-count { font-size: 1rem; }
        }

        /* Off-strip / off-screen panels: every ambience layer holds its frame.
           (See the IntersectionObserver above.) */
        .rw-strip-item.rw-idle .rw-marble,
        .rw-strip-item.rw-idle .rw-marble-flow,
        .rw-strip-item.rw-idle .rw-brick-bg,
        .rw-strip-item.rw-idle .rw-land-bg,
        .rw-strip-item.rw-idle .rw-float-device,
        .rw-strip-item.rw-idle .rw-still-art,
        .rw-strip-item.rw-idle .rw-crt-ghost {
          animation-play-state: paused;
        }

        /* Reduced motion: kill all decorative animation + smooth scroll. */
        @media (prefers-reduced-motion: reduce) {
          .rw-marble,
          .rw-marble-flow,
          .rw-brick-bg,
          .rw-land-bg,
          .rw-float-device { animation: none !important; }
          .rw-still-art { animation: none !important; transform: none !important; }
          /* Scanlines stay (they're static texture); the glitch tick does not. */
          .rw-crt-ghost { animation: none !important; opacity: 0 !important; }
          .rw-progress-bar { transition: none !important; }
        }
      `})]})}export{Z as default};
