const __vite__mapDeps=(i,m=__vite__mapDeps,d=(m.f||(m.f=["_astro/CaseStudyOverlay.Dx0GcvkB.js","_astro/jsx-runtime.D_zvdyIk.js","_astro/index.DiEladB3.js","_astro/index.CB87Sc6I.js","_astro/CaseStudyContent.oUWCQ6Op.js","_astro/preload-helper.BlTxHScW.js"])))=>i.map(i=>d[i]);
import{_ as B}from"./preload-helper.BlTxHScW.js";import{j as e}from"./jsx-runtime.D_zvdyIk.js";import{r as s}from"./index.DiEladB3.js";import{r as I}from"./index.UCiZe19v.js";var O=I();const E=()=>B(()=>import("./CaseStudyOverlay.Dx0GcvkB.js"),__vite__mapDeps([0,1,2,3,4,5])),$=s.lazy(E);let N=!1;const F=()=>{N||(N=!0,E())},x=[{slug:"garcia",index:"01",name:"Garcia HandPicked",category:"Packaging · Flower / Edibles / Pre-rolls",position:"A Jerry Garcia-licensed cannabis brand that puts psychedelic concert-poster art on the shelf.",blurb:"Grateful Dead concert-poster energy meets compliant cannabis packaging. Fluid rainbow liquid-marble artwork and an integrated Jerry Garcia portrait carry a deep SKU range and strain-coded variants.",hero:"/portfolio/garcia/garcia-brand-cover-1x1.webp",heroW:1080,heroH:1080,heroPos:"50% 20%",effect:"still",square:!0,caseStudy:!0,bg:"#080608",accent:"#ff4fd8",accentText:"#0a0a0a",glow:"rgba(255,120,220,0.35)"},{slug:"strane",index:"02",name:"Strane",category:"Branding · Disposable Vapes & Cartridges",position:"Cannabis vapes with a punk streak: street-art grit meets clean device renders.",blurb:"A distinctly punk visual system: distressed brick, paint splatter and retro collage against hard acid-yellow-and-black, while premium 3D device renders keep it product-forward.",hero:"/portfolio/strane/strane-brand-cover-1x1.webp",heroW:1080,heroH:1080,heroPos:"24% 15%",effect:"still",square:!0,caseStudy:!0,bg:"#0b0b06",accent:"#e5ff00",accentText:"#0a0a0a",glow:"rgba(229,255,0,0.30)"},{slug:"avitas",index:"03",name:"AVITAS",category:"Branding · Vape Cartridges & Gummies",position:"A premium, nature-forward cartridge system built on illustrated landscapes.",blurb:"A full identity system where each tier owns an illustrated landscape: mountains, ocean and botanicals in turquoise-to-navy with gold accents, anchored by a distinctive geometric “A” mark.",hero:"/portfolio/avitas/avitas-brand-cover-1x1.webp",heroW:1080,heroH:1080,heroPos:"50% 84%",effect:"still",square:!0,caseStudy:!0,bg:"#04141c",accent:"#e8c87a",accentText:"#0a0a0a",glow:"rgba(64,200,210,0.30)"},{slug:"hellavated",index:"04",name:"Hellavated",category:"Packaging · Edibles, Devices & Beverages",position:"A vibrant, compliance-forward edibles-and-devices brand built for Gen-Z.",blurb:"Fruit-illustration hero panels meet a black technical compliance side, a two-tone system spanning 18+ gummy flavors, CloudBar vape devices and the Baja beverage line.",hero:"/portfolio/hellavated/hellavated-brand-cover-1x1.webp",heroW:1080,heroH:1080,heroPos:"50% 84%",effect:"still",square:!0,caseStudy:!0,bg:"#070707",accent:"#b6ff2e",accentText:"#0a0a0a",glow:"rgba(182,255,46,0.30)"},{slug:"lootbar",index:"05",name:"Lootbar",category:"Packaging · Disposable Vapes & Key Art",position:"Vaporwave flavor system for LightHouse, where every flavor is a loot drop.",blurb:"Every flavor staged as an open treasure chest with the device floating beside it as the prize, inside one neon HUD frame that recolors across 11 key-arts. Y2K chrome lettering, pixel hearts and arcade checkerboards run throughout.",hero:"/portfolio/lootbar/hi/lootbar-blueberry-dream.webp",heroW:1200,heroH:1200,effect:"crt",square:!0,caseStudy:!0,bg:"#02040e",accent:"#00e0f0",accentText:"#0a0a0a",glow:"rgba(0,224,240,0.30)"},{slug:"better",index:"06",name:"BETTER",category:"Packaging · Cartridges & Gummies",position:"An occasion-based cannabis system that colors by time of day: Days, Sleep, Anytime.",blurb:"Three occasions, three worlds: sunset orange for Sunshine Tangerine days, midnight navy for Midnight Mixed Berry sleep, lavender-and-lemon for Easy Lemon Lavender anytime. Script sub-wordmarks sit under one bold BETTER sans, from blister-carded cartridges to gummy pouches.",hero:"/portfolio/better/hi/better-pink-picasso-hero-1x1.webp",heroW:1898,heroH:1898,effect:"still",square:!0,caseStudy:!0,bg:"#15060f",accent:"#ff4fa3",accentText:"#0a0a0a",glow:"rgba(255,79,163,0.32)"},{slug:"zeife",index:"07",name:"Zëife",category:"Branding · Soaps & Aromatics",position:"A crescent-moon-and-lotus identity for a soap & aromatics house, the first non-cannabis brand in the set.",blurb:"One pink mark, a crescent moon cradling a lotus, set in Pantone-specced moon-dust gray and lotus pink: debossed into black card stock, screened onto shopping bags, framed art-nouveau on business cards. Identity first; the product line comes next.",hero:"/portfolio/zeife/zeife-logo-debossed-1x1.webp",heroW:1080,heroH:1080,effect:"still",square:!0,caseStudy:!1,bg:"#0d090b",accent:"#e9a7b8",accentText:"#0a0a0a",glow:"rgba(233,167,184,0.26)"},{slug:"dougie",index:"08",name:"Dougie",category:"Packaging · Infused Blunts & POS",position:"Graffiti-wall packaging for cannabis-infused blunts, distributed under LightHouse.",blurb:"One gold wordmark over a scratched-up graffiti wall that recolors by strain: green for hybrid, purple for indica, orange for sativa, across one-gram and two-gram pouches. A ripped-paper table tent carries the same wall onto the dispensary counter under the line “Make More Mischief.”",hero:"/portfolio/dougie/dougie-strain-trio-trim.webp",heroW:1142,heroH:1159,effect:"float",square:!0,bloom:"rgba(255,202,79,0.15)",caseStudy:!1,bg:"#0a0c06",accent:"#ffca4f",accentText:"#0a0a0a",glow:"rgba(156,215,100,0.28)"},{slug:"ardent",index:"09",name:"Ardent",category:"Packaging · Infusion & Edibles Kits",position:"A kit line where every recipe gets its own color, pattern and hand-painted plate.",blurb:"Each corrugated mailer tiles the lowercase swirl monogram with the recipe’s own motif: cocoa pods for Mini Truffle Cups, apples for Apple Pie. A textured paper label fronts the box with a watercolor plate of the finished bake, and the wordmark repeats inside the lid. Shown across Apple Pie, Olive Oil, Birthday Cupcakes, Breakfast Cloud Cakes and Mini Truffle Cups.",hero:"/portfolio/ardent/ardent-truffle-cups-kit.webp",heroW:1400,heroH:1400,effect:"still",square:!0,caseStudy:!1,bg:"#120c09",accent:"#7ea6e0",accentText:"#0a0a0a",glow:"rgba(126,166,224,0.24)"}],y="#FF5A1F",G=(t,u)=>({src:t.hero,width:t.heroW,height:t.heroH,decoding:"async",draggable:!1,...u?{fetchPriority:"high"}:{loading:"lazy"}});function V({p:t,priority:u}){const l=G(t,u);return t.effect==="float"?e.jsx("div",{className:"rw-float",style:t.bloom?{"--rw-bloom":t.bloom}:void 0,children:e.jsx("img",{...l,alt:`${t.name} device`,className:"rw-float-device"})}):t.effect==="brick"?e.jsxs("div",{className:"rw-brick",children:[t.brick&&e.jsx("div",{className:"rw-brick-bg",style:{backgroundImage:`url(${t.brick})`},"aria-hidden":"true"}),e.jsx("div",{className:"rw-brick-splatter","aria-hidden":"true"}),e.jsx("img",{...l,alt:`${t.name} device`,className:"rw-brick-device"})]}):t.effect==="crt"?e.jsxs("div",{className:"rw-crt",children:[e.jsx("img",{...l,alt:`${t.name} key visual`,className:"rw-crt-art"}),e.jsx("div",{className:"rw-crt-ghost rw-crt-ghost-r","aria-hidden":"true",children:e.jsx("img",{...l,alt:"",loading:"lazy"})}),e.jsx("div",{className:"rw-crt-ghost rw-crt-ghost-c","aria-hidden":"true",children:e.jsx("img",{...l,alt:"",loading:"lazy"})}),e.jsx("div",{className:"rw-crt-lines","aria-hidden":"true"}),e.jsx("div",{className:"rw-crt-vig","aria-hidden":"true"})]}):t.effect==="still"?e.jsxs("div",{className:"rw-still",children:[e.jsx("img",{...l,alt:`${t.name} key visual`,className:t.heroPos?"rw-still-art rw-still-art-tight":"rw-still-art",style:t.heroPos?{objectPosition:t.heroPos}:void 0}),e.jsx("div",{className:"rw-still-vig","aria-hidden":"true"})]}):t.effect==="landscape"?e.jsxs("div",{className:"rw-land",children:[t.back&&e.jsx("div",{className:"rw-land-bg",style:{backgroundImage:`url(${t.back})`},"aria-hidden":"true"}),e.jsx("div",{className:"rw-land-vig","aria-hidden":"true"}),e.jsx("img",{...l,alt:`${t.name} landscape`,className:"rw-land-img"})]}):e.jsxs("div",{className:"rw-marble-wrap",children:[e.jsx("div",{className:"rw-marble","aria-hidden":"true",children:e.jsx("div",{className:"rw-marble-flow"})}),e.jsx("img",{...l,alt:`${t.name} hero`,className:"rw-marble-img"})]})}function K({p:t,priority:u,onOpen:l}){return e.jsxs("article",{className:t.square?"rw-panel rw-panel-square":"rw-panel",onPointerOver:t.caseStudy?F:void 0,style:{background:`radial-gradient(120% 120% at 100% 0%, ${t.glow} 0%, transparent 45%), ${t.bg}`,borderColor:"rgba(255,255,255,0.08)"},children:[e.jsxs("div",{className:"rw-panel-visual",children:[e.jsx(V,{p:t,priority:u}),e.jsx("span",{className:"rw-panel-index",style:{color:t.accent},children:t.index})]}),e.jsxs("div",{className:"rw-panel-copy",children:[e.jsx("span",{className:"rw-panel-cat",style:{color:t.accent,borderColor:"rgba(255,255,255,0.14)"},children:t.category}),e.jsx("h3",{className:"rw-panel-name",children:t.name}),e.jsx("p",{className:"rw-panel-pos",children:t.position}),e.jsx("p",{className:"rw-panel-blurb",children:t.blurb}),t.caseStudy?e.jsxs("a",{href:`/work/${t.slug}`,"data-project":t.slug,className:"rw-panel-cta",style:{color:t.accent},onClick:g=>l(t.slug,g),children:[e.jsx("span",{children:"View Case Study"}),e.jsx("svg",{width:"14",height:"14",viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2.5",strokeLinecap:"round",strokeLinejoin:"round",children:e.jsx("path",{d:"M7 17L17 7M17 7H7M17 7V17"})})]}):e.jsx("span",{className:"rw-panel-cta rw-panel-cta-soon","aria-disabled":"true",children:e.jsx("span",{children:"Case Study Soon"})})]}),t.caseStudy&&e.jsx("a",{href:`/work/${t.slug}`,className:"rw-panel-hit",tabIndex:-1,"aria-hidden":"true",onClick:g=>l(t.slug,g)})]})}function Z(){const t=s.useRef(null),u=s.useRef(null),l=s.useRef(1),g=s.useRef(()=>{}),[L,C]=s.useState(1),[T,A]=s.useState(!0),[z,P]=s.useState(!1),[S,w]=s.useState(null),b=s.useRef(!1),q=s.useCallback((r,a)=>{a.defaultPrevented||a.metaKey||a.ctrlKey||a.shiftKey||a.altKey||a.button!==0||(a.preventDefault(),history.pushState({csOverlay:!0},"",`/work/${r}`),b.current=!0,w(r))},[]);s.useEffect(()=>{const r=()=>{const a=window.location.pathname.match(/^\/work\/([\w-]+)\/?$/),n=a&&x.find(i=>i.slug===a[1]&&i.caseStudy);n?(b.current=!0,w(n.slug)):(b.current=!1,w(null))};return window.addEventListener("popstate",r),()=>window.removeEventListener("popstate",r)},[]);const M=s.useCallback(()=>{b.current?history.back():w(null)},[]),R=s.useCallback(r=>{history.replaceState({csOverlay:!0},"",`/work/${r}`),w(r)},[]),W=r=>{const a=r.querySelector(".rw-strip-item");if(!a)return r.clientWidth||1;const n=parseFloat(getComputedStyle(r).columnGap||"0")||0;return a.getBoundingClientRect().width+n};s.useEffect(()=>{const r=t.current;if(!r)return;let a=-1,n=null,i=null;const h=()=>{l.current=W(r)||1},p=()=>{const d=r.scrollWidth-r.clientWidth,o=r.scrollLeft;if(u.current){const H=d>0?o/d:0;u.current.style.transform=`scaleX(${Math.min(1,Math.max(0,H))})`}const m=Math.min(x.length,Math.round(o/l.current)+1);m!==a&&(a=m,C(m));const k=o<=1;k!==n&&(n=k,A(k));const j=o>=d-1;j!==i&&(i=j,P(j))};h(),p(),r.addEventListener("scroll",p,{passive:!0});const f=()=>{h(),p()};return window.addEventListener("resize",f),r.querySelectorAll("img").forEach(d=>{d.complete||d.addEventListener("load",f,{once:!0})}),()=>{r.removeEventListener("scroll",p),window.removeEventListener("resize",f)}},[]),s.useEffect(()=>{const r=t.current;if(!r)return;const a=Array.from(r.querySelectorAll(".rw-strip-item"));if(!a.length)return;const n=new IntersectionObserver(i=>{for(const h of i)h.target.classList.toggle("rw-idle",!h.isIntersecting)},{rootMargin:"400px 400px"});return a.forEach(i=>n.observe(i)),()=>n.disconnect()},[]),s.useEffect(()=>{const r=t.current;if(!r)return;let a=!1,n=!1,i=0,h=0;const p=o=>{o.pointerType==="touch"||o.button!==0||(g.current(),a=!0,n=!1,i=o.clientX,h=r.scrollLeft)},f=o=>{if(!a)return;const m=o.clientX-i;!n&&Math.abs(m)>4&&(n=!0,r.classList.add("rw-dragging"),r.setPointerCapture?.(o.pointerId),window.getSelection()?.removeAllRanges()),n&&(r.scrollLeft=h-m,o.preventDefault())},c=()=>{a=!1,r.classList.remove("rw-dragging")},d=o=>{n&&(o.preventDefault(),o.stopPropagation(),n=!1)};return r.addEventListener("pointerdown",p),r.addEventListener("pointermove",f),window.addEventListener("pointerup",c),window.addEventListener("pointercancel",c),r.addEventListener("click",d,!0),()=>{r.removeEventListener("pointerdown",p),r.removeEventListener("pointermove",f),window.removeEventListener("pointerup",c),window.removeEventListener("pointercancel",c),r.removeEventListener("click",d,!0)}},[]),s.useEffect(()=>{const r=t.current;if(!r)return;const a=window.matchMedia("(prefers-reduced-motion: reduce)").matches;let n=r.scrollLeft,i=0;const h=()=>{i&&cancelAnimationFrame(i),i=0,n=r.scrollLeft};g.current=h;const p=()=>{const c=r.scrollWidth-r.clientWidth;n=Math.max(0,Math.min(c,n));const d=r.scrollLeft,o=n-d;if(Math.abs(o)<1){r.scrollLeft=n,i=0;return}if(r.scrollLeft=d+o*.18,r.scrollLeft===d){i=0;return}i=requestAnimationFrame(p)},f=c=>{if(!(Math.abs(c.deltaX)<=Math.abs(c.deltaY))){if(c.preventDefault(),c.stopPropagation(),a){r.scrollLeft+=c.deltaX;return}i||(n=r.scrollLeft),n+=c.deltaX,i||(i=requestAnimationFrame(p))}};return r.addEventListener("wheel",f,{passive:!1}),()=>{r.removeEventListener("wheel",f),i&&cancelAnimationFrame(i),g.current=()=>{}}},[]);const v=r=>{const a=t.current;a&&(g.current(),a.scrollBy({left:r*l.current,behavior:"smooth"}))},D=r=>{r.key==="ArrowRight"?(r.preventDefault(),v(1)):r.key==="ArrowLeft"&&(r.preventDefault(),v(-1))};return e.jsxs("section",{id:"work",className:"rw-section relative z-20",style:{background:"#050505"},children:[e.jsxs("div",{className:"rw-wrap",children:[e.jsxs("div",{className:"rw-head",children:[e.jsx("p",{className:"rw-eyebrow",children:"Recent Work"}),e.jsxs("h2",{className:"rw-title",children:["Brands, with ",e.jsx("span",{style:{color:y},children:"conviction"}),"."]}),e.jsx("p",{className:"rw-intro",children:"Each its own world, built end to end."})]}),e.jsx("div",{className:"w-full overflow-hidden",children:e.jsx("div",{ref:t,className:"rw-strip",role:"group","aria-roledescription":"carousel","aria-label":"Recent work",tabIndex:0,onKeyDown:D,children:x.map((r,a)=>e.jsx("div",{className:"rw-strip-item",children:e.jsx(K,{p:r,priority:a===0,onOpen:q})},r.slug))})}),e.jsxs("div",{className:"rw-foot",children:[e.jsxs("div",{className:"rw-count",children:[e.jsx("span",{style:{color:y},children:String(L).padStart(2,"0")}),e.jsx("span",{className:"rw-count-sep",children:"/"}),e.jsx("span",{children:String(x.length).padStart(2,"0")})]}),e.jsx("div",{className:"rw-progress-track",children:e.jsx("div",{ref:u,className:"rw-progress-bar",style:{background:`linear-gradient(90deg, ${y}, #FFA040)`}})}),e.jsxs("div",{className:"rw-arrows",children:[e.jsx("button",{type:"button",className:"rw-arrow","aria-label":"Previous project",onClick:()=>v(-1),disabled:T,children:e.jsx("svg",{width:"18",height:"18",viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2.5",strokeLinecap:"round",strokeLinejoin:"round",children:e.jsx("path",{d:"M15 18l-6-6 6-6"})})}),e.jsx("button",{type:"button",className:"rw-arrow","aria-label":"Next project",onClick:()=>v(1),disabled:z,children:e.jsx("svg",{width:"18",height:"18",viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2.5",strokeLinecap:"round",strokeLinejoin:"round",children:e.jsx("path",{d:"M9 6l6 6-6 6"})})})]})]})]}),S&&O.createPortal(e.jsx(s.Suspense,{fallback:null,children:e.jsx($,{slug:S,onClose:M,onNext:R})}),document.body),e.jsx("style",{children:`
        .rw-section { color: #fff; }
        .rw-wrap { padding: 3.5rem 0 3rem; }

        /* ── Header ── */
        .rw-head { padding: 0 3rem 1.75rem; }
        .rw-eyebrow {
          font-family: 'Montserrat', sans-serif;
          font-size: 0.7rem;
          font-weight: 600;
          letter-spacing: 0.35em;
          text-transform: uppercase;
          color: rgba(255,255,255,0.32);
          margin-bottom: 0.9rem;
        }
        .rw-title {
          font-family: 'Montserrat', sans-serif;
          font-weight: 900;
          text-transform: uppercase;
          line-height: 0.95;
          letter-spacing: -0.01em;
          font-size: clamp(2rem, 4.5vw, 4rem);
          color: #fff;
        }
        .rw-intro {
          margin-top: 0.9rem;
          max-width: 34rem;
          color: rgba(255,255,255,0.42);
          font-size: 0.95rem;
          line-height: 1.5;
        }

        /* ── Native horizontal-scroll strip ── */
        .rw-strip {
          --rw-card: min(72vw, 1280px);
          /* Named because .rw-panel-square has to reason about it: a square
             visual half is exactly as wide as the panel is tall. */
          --rw-card-h: clamp(420px, 60vh, 560px);
          display: flex;
          gap: 2rem;
          overflow-x: auto;
          overflow-y: hidden;
          overscroll-behavior-x: contain;
          -webkit-overflow-scrolling: touch;
          scrollbar-width: none;
          /* Center the first card; the symmetric inline padding leaves the next
             card peeking on the right, and centers each card under the arrows. */
          padding-block: 0.5rem 1.5rem;
          padding-inline: max(1.5rem, calc((100vw - var(--rw-card)) / 2));
          cursor: grab;
        }
        .rw-strip::-webkit-scrollbar { display: none; }
        .rw-strip:focus-visible {
          outline: 2px solid ${y};
          outline-offset: 4px;
        }
        .rw-strip.rw-dragging {
          cursor: grabbing;
          scroll-behavior: auto;
          user-select: none;
          -webkit-user-select: none;
        }
        .rw-strip.rw-dragging * {
          user-select: none;
          -webkit-user-select: none;
        }
        .rw-strip.rw-dragging .rw-panel { pointer-events: none; }
        .rw-strip-item {
          flex: 0 0 auto;
          width: var(--rw-card);
          height: var(--rw-card-h);
        }

        /* ── Footer: counter + progress + arrows ── */
        .rw-foot {
          display: flex;
          align-items: center;
          gap: 1.5rem;
          padding: 1rem 3rem 0;
        }
        .rw-count {
          font-family: 'Montserrat', sans-serif;
          font-weight: 800;
          font-size: 0.9rem;
          letter-spacing: 0.1em;
          color: rgba(255,255,255,0.35);
          display: flex;
          gap: 0.35rem;
        }
        .rw-count-sep { color: rgba(255,255,255,0.2); }
        .rw-progress-track {
          flex: 1 1 auto;
          height: 3px;
          border-radius: 3px;
          background: rgba(255,255,255,0.1);
          overflow: hidden;
        }
        .rw-progress-bar {
          height: 100%;
          width: 100%;
          transform: scaleX(0);
          transform-origin: left center;
          border-radius: 3px;
          transition: transform 0.1s linear;
        }
        .rw-arrows { display: flex; gap: 0.6rem; flex: none; }
        .rw-arrow {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          width: 2.75rem;
          height: 2.75rem;
          border-radius: 999px;
          border: 1px solid rgba(255,255,255,0.16);
          background: rgba(255,255,255,0.03);
          color: #fff;
          cursor: pointer;
          transition: background 0.2s ease, border-color 0.2s ease, opacity 0.2s ease, transform 0.1s ease;
        }
        .rw-arrow:hover:not(:disabled) {
          background: rgba(255,255,255,0.09);
          border-color: rgba(255,255,255,0.32);
        }
        .rw-arrow:active:not(:disabled) { transform: scale(0.94); }
        .rw-arrow:disabled { opacity: 0.28; cursor: default; }

        /* ── Panel ── */
        .rw-panel {
          position: relative;
          width: 100%;
          height: 100%;
          border: 1px solid rgba(255,255,255,0.08);
          border-radius: 1.75rem;
          overflow: hidden;
          display: grid;
          grid-template-columns: 1.05fr 0.95fr;
          box-shadow: 0 30px 90px rgba(0,0,0,0.55);
        }
        /* Square masters (Quynh's 1:1 promo comps) get a square hole instead of
           the default 1.05fr one, so object-fit: cover has nothing to trim and
           the whole comp survives. The width is the panel's inner height —
           --rw-card-h less the 1px borders, which count because Tailwind's
           preflight puts everything in border-box. The copy half takes the rest,
           so it ends up WIDER here than on the other panels, not narrower. */
        .rw-panel-square {
          grid-template-columns: calc(var(--rw-card-h) - 2px) 1fr;
        }
        .rw-panel-visual {
          position: relative;
          overflow: hidden;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 2.5rem;
        }
        .rw-panel-index {
          position: absolute;
          top: 1.5rem;
          left: 1.75rem;
          font-family: 'Montserrat', sans-serif;
          font-weight: 900;
          font-size: 2.75rem;
          line-height: 1;
          opacity: 0.9;
          z-index: 5;
          text-shadow: 0 2px 20px rgba(0,0,0,0.5);
        }
        .rw-panel-copy {
          position: relative;
          z-index: 4;
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          justify-content: center;
          gap: 1rem;
          padding: 3rem;
          background: linear-gradient(90deg, transparent, rgba(0,0,0,0.35) 30%);
        }
        .rw-panel-cat {
          font-family: 'Montserrat', sans-serif;
          font-size: 0.65rem;
          font-weight: 700;
          letter-spacing: 0.18em;
          text-transform: uppercase;
          padding: 0.4rem 0.8rem;
          border: 1px solid;
          border-radius: 999px;
        }
        .rw-panel-name {
          font-family: 'Montserrat', sans-serif;
          font-weight: 900;
          text-transform: uppercase;
          font-size: clamp(1.8rem, 3vw, 3rem);
          line-height: 0.98;
          color: #fff;
        }
        .rw-panel-pos {
          font-size: clamp(1rem, 1.4vw, 1.35rem);
          font-weight: 600;
          line-height: 1.3;
          color: rgba(255,255,255,0.9);
          max-width: 26rem;
        }
        .rw-panel-blurb {
          font-size: 0.92rem;
          line-height: 1.6;
          color: rgba(255,255,255,0.55);
          max-width: 27rem;
        }
        .rw-panel-cta {
          display: inline-flex;
          align-items: center;
          gap: 0.5rem;
          margin-top: 0.5rem;
          font-family: 'Montserrat', sans-serif;
          font-size: 0.72rem;
          font-weight: 700;
          letter-spacing: 0.16em;
          text-transform: uppercase;
          transition: gap 0.25s ease, opacity 0.25s ease;
        }
        .rw-panel-cta:hover { gap: 0.85rem; opacity: 0.85; }
        /* The whole card is the click target, so the CTA answers to a hover
           anywhere on the card — that arrow sliding out is the only signal
           the card is live. */
        .rw-panel:hover .rw-panel-cta:not(.rw-panel-cta-soon) {
          gap: 0.85rem;
          opacity: 0.85;
        }
        .rw-panel-hit {
          position: absolute;
          inset: 0;
          z-index: 6;
          /* inherit, not pointer: the strip's grab/grabbing cursor is the drag
             affordance and it stays the primary gesture here. */
          cursor: inherit;
        }
        .rw-panel-cta-soon {
          color: rgba(255,255,255,0.32);
          cursor: default;
        }
        .rw-panel-cta-soon:hover { gap: 0.5rem; opacity: 1; }

        /* Cutout stages (marble / brick / landscape / float) are absolute like
           .rw-crt and .rw-still, so their art ignores .rw-panel-visual's padding
           and reaches the panel edge. The subject then measures against the WHOLE
           half — a cutout at 100% here is as big as the full-bleed art on the
           Lootbar / BETTER / Ardent panels, which is the look those three get. */
        .rw-marble-wrap,
        .rw-brick,
        .rw-land,
        .rw-float {
          position: absolute;
          inset: 0;
          overflow: hidden;
          display: flex;
          align-items: center;
          justify-content: center;
        }

        /* ── Garcia: liquid-marble ──
           The turn/breathe half of the motion. The colour band that slides
           across it is .rw-marble-flow, a child, because the slide used to be
           an animated background-position — and background-position is not a
           compositor property, so every frame re-rasterised a 40px-blurred
           conic gradient on the main thread, for a panel that is off screen
           most of the time. */
        .rw-marble {
          position: absolute;
          inset: -30%;
          opacity: 0.55;
          animation: rw-marble-turn 18s ease-in-out infinite;
          will-change: transform;
        }
        /* Same pixels, expressed as a translate. background-size: 200% 200%
           painted a 2W x 2H gradient behind the W x H layer and background-
           position slid it from x=0 to x=-W; this layer IS 2W wide, shows the
           gradient's middle horizontal band (100% 200% at 0% 50% — still a
           2W x 2H image), and travels the same -W, which is -50% of its own
           width. The parent's rotate/scale then applies on top, exactly as it
           did when the gradient was the parent's own background. */
        .rw-marble-flow {
          position: absolute;
          top: 0;
          left: 0;
          width: 200%;
          height: 100%;
          background: conic-gradient(from 0deg,
            #ff0040, #ff8a00, #ffe600, #37ff5b, #00c2ff, #7a3bff, #ff2fd0, #ff0040);
          background-size: 100% 200%;
          background-position: 0% 50%;
          filter: blur(40px) saturate(1.3);
          animation: rw-marble-flow 18s ease-in-out infinite;
          will-change: transform;
        }
        .rw-marble-img {
          position: relative;
          z-index: 2;
          height: 94%;
          width: auto;
          max-width: 88%;
          object-fit: contain;
          filter: drop-shadow(0 20px 40px rgba(0,0,0,0.5));
        }
        @keyframes rw-marble-turn {
          0%   { transform: rotate(0deg) scale(1); }
          50%  { transform: rotate(8deg) scale(1.12); }
          100% { transform: rotate(0deg) scale(1); }
        }
        @keyframes rw-marble-flow {
          0%   { transform: translate3d(0, 0, 0); }
          50%  { transform: translate3d(-50%, 0, 0); }
          100% { transform: translate3d(0, 0, 0); }
        }

        /* ── Strane: brick backdrop + device ── */
        .rw-brick-bg {
          position: absolute;
          inset: 0;
          background-size: cover;
          background-position: center;
          opacity: 0.55;
          animation: rw-brick-drift 24s ease-in-out infinite alternate;
        }
        .rw-brick-splatter {
          position: absolute;
          inset: 0;
          background:
            radial-gradient(circle at 20% 30%, rgba(229,255,0,0.25) 0, transparent 12%),
            radial-gradient(circle at 78% 70%, rgba(229,255,0,0.18) 0, transparent 16%),
            radial-gradient(circle at 60% 15%, rgba(229,255,0,0.12) 0, transparent 9%);
          mix-blend-mode: screen;
        }
        /* Trimmed render, so 100% height IS the shelf display: it stands the
           full height of the half, like a product on a shelf, instead of hovering
           at 46% of the width. */
        .rw-brick-device {
          position: relative;
          z-index: 2;
          height: 100%;
          width: auto;
          max-width: 92%;
          object-fit: contain;
          filter: drop-shadow(0 24px 44px rgba(0,0,0,0.7));
        }
        /* This one keeps its background-position term. Unlike the marble, the
           distance it travels is 12% of (layer width − COVER width), and cover
           width depends on the panel's aspect ratio at the current viewport —
           there is no transform that equals it at every size, so re-expressing
           it would be an approximation, not a translation. Measured: the term
           resolves to exactly 0 at the common desktop sizes (the layer is wider
           than 773:800, so cover fits width and there is no horizontal slack),
           ~7px at 1024x768 and ~0.5px on a phone. It is a cheap repaint too —
           an unblurred bitmap, not the marble's blurred conic — and off-screen
           panels no longer run it at all. */
        @keyframes rw-brick-drift {
          from { background-position: 0% 50%; transform: scale(1.05); }
          to   { background-position: 12% 50%; transform: scale(1.12); }
        }

        /* ── AVITAS: ken-burns landscape ── */
        .rw-land {
          background:
            radial-gradient(120% 90% at 50% 10%, rgba(64,200,210,0.22), transparent 60%);
        }
        /* The ken-burns moved off the pack and onto the landscape behind it: a
           product render that slowly grows and slides reads as a glitch, an
           illustrated backdrop that does is the effect this panel is named for. */
        .rw-land-bg {
          position: absolute;
          inset: 0;
          background-size: cover;
          background-position: 50% 55%;
          opacity: 0.72;
          animation: rw-kenburns 26s ease-in-out infinite alternate;
        }
        /* Same edge fade the crt / still panels use, so the illustration darkens
           into the panel instead of ending on a hard line. */
        .rw-land-vig {
          position: absolute;
          inset: 0;
          pointer-events: none;
          background:
            radial-gradient(115% 105% at 45% 50%, transparent 45%, rgba(4,20,28,0.85) 100%),
            linear-gradient(90deg, transparent 66%, rgba(4,20,28,0.9) 100%);
        }
        .rw-land-img {
          position: relative;
          z-index: 2;
          height: 92%;
          width: auto;
          max-width: 88%;
          object-fit: contain;
          filter: drop-shadow(0 26px 46px rgba(0,0,0,0.6));
        }
        @keyframes rw-kenburns {
          from { transform: scale(1.04) translateY(0); }
          to   { transform: scale(1.14) translateY(-1.5%); }
        }

        /* ── Dougie: floating cutout render ── */
        .rw-float {
          background:
            radial-gradient(120% 90% at 50% 12%, var(--rw-bloom, rgba(182,255,46,0.16)), transparent 62%);
        }
        /* Trimmed render, so this is sized against the whole half and the trio
           runs the full height. The bob is translate-only — a scale on a
           100%-sized cutout would clip it against the panel edge at the top of
           the cycle. */
        .rw-float-device {
          position: relative;
          z-index: 2;
          max-height: 96%;
          max-width: 92%;
          object-fit: contain;
          filter: drop-shadow(0 22px 42px rgba(0,0,0,0.6));
          animation: rw-float-bob 9s ease-in-out infinite alternate;
        }
        @keyframes rw-float-bob {
          from { transform: translateY(0.7%); }
          to   { transform: translateY(-0.7%); }
        }

        /* ── Lootbar: full-bleed key art under a CRT treatment ── */
        /* Absolute, so the art ignores .rw-panel-visual's padding and bleeds
           to the panel edge — this hero is composed key art, not a cutout. */
        .rw-crt { position: absolute; inset: 0; overflow: hidden; }
        /* Fills the whole half, like every other panel. This used to be a
           72%-tall letterboxed band because the hero was a 16:9 campaign
           visual that a near-square cover crop destroyed; the hero is now a
           1200x1200 flavor key art, so it fills edge to edge with ~1% crop and
           there is no dead ground above or below it. */
        .rw-crt-art,
        .rw-crt-ghost img {
          position: absolute;
          inset: 0;
          width: 100%;
          height: 100%;
          object-fit: cover;
          object-position: 50% 50%;
          display: block;
        }
        /* Channel ghosts: the art multiplied down to one channel, screened
           back over the base. Idle at opacity 0 — only the tick reveals them. */
        /* The ghost box tracks the art box exactly — its tint fills its own
           box, so any mismatch would flood the gap with flat colour. */
        .rw-crt-ghost {
          position: absolute;
          inset: 0;
          mix-blend-mode: screen;
          opacity: 0;
          pointer-events: none;
        }
        .rw-crt-ghost img { inset: 0; height: 100%; mix-blend-mode: multiply; }
        .rw-crt-ghost-r { background: #ff2e55; animation: rw-crt-tick-r 7.2s steps(1, end) infinite; }
        .rw-crt-ghost-c { background: #2ee6ff; animation: rw-crt-tick-c 7.2s steps(1, end) infinite; }
        /* ~120ms of split every 7.2s. Deliberately weak: at 0.75 opacity the
           art washed out completely — this is a fringe, not a takeover. */
        @keyframes rw-crt-tick-r {
          0%,     1.09%  { opacity: 0.34; transform: translate3d(-3px, 1px, 0); }
          1.1%,   1.68%  { opacity: 0.22; transform: translate3d(2px, -1px, 0); }
          1.69%, 100%    { opacity: 0; transform: none; }
        }
        @keyframes rw-crt-tick-c {
          0%,     1.09%  { opacity: 0.34; transform: translate3d(3px, -1px, 0); }
          1.1%,   1.68%  { opacity: 0.22; transform: translate3d(-2px, 1px, 0); }
          1.69%, 100%    { opacity: 0; transform: none; }
        }
        .rw-crt-lines {
          position: absolute;
          inset: 0;
          pointer-events: none;
          background: repeating-linear-gradient(
            0deg,
            rgba(0,0,0,0.30) 0px,
            rgba(0,0,0,0.30) 1px,
            transparent 1px,
            transparent 3px
          );
        }
        .rw-crt-vig {
          position: absolute;
          inset: 0;
          pointer-events: none;
          background:
            radial-gradient(115% 105% at 50% 45%, transparent 58%, rgba(2,4,14,0.62) 100%),
            linear-gradient(90deg, transparent 72%, rgba(2,4,14,0.55) 100%);
        }

        /* ── Hellavated / BETTER / Zeife: full-bleed opaque comp, slow ken-burns ── */
        /* Absolute like .rw-crt, so the art ignores .rw-panel-visual's padding
           and bleeds to the panel edge — these are composed photographs with
           baked backgrounds, not cutouts. */
        .rw-still { position: absolute; inset: 0; overflow: hidden; }
        .rw-still-art {
          position: absolute;
          inset: 0;
          width: 100%;
          height: 100%;
          object-fit: cover;
          object-position: 50% 50%;
          display: block;
          transform-origin: 50% 50%;
          animation: rw-still-drift 48s ease-in-out infinite alternate;
          will-change: transform;
        }
        /* Art with a wordmark near the crop edge (Hellavated's cover) gets a
           gentler drift — the default 1.09 zoom plus translate walks the logo
           straight off the bottom of the half. */
        .rw-still-art-tight { animation-name: rw-still-drift-tight; }
        @keyframes rw-still-drift-tight {
          from { transform: scale(1.01) translate3d(0, 0, 0); }
          to   { transform: scale(1.045) translate3d(-0.5%, 0, 0); }
        }
        @keyframes rw-still-drift {
          from { transform: scale(1.02) translate3d(0, 0, 0); }
          to   { transform: scale(1.09) translate3d(-1.2%, -1%, 0); }
        }
        .rw-still-vig {
          position: absolute;
          inset: 0;
          pointer-events: none;
          background:
            radial-gradient(115% 105% at 50% 45%, transparent 58%, rgba(0,0,0,0.55) 100%),
            linear-gradient(90deg, transparent 72%, rgba(0,0,0,0.5) 100%);
        }

        /* ── Tablet / mobile: stack the panel, shrink the strip ── */
        @media (max-width: 1023px) {
          .rw-wrap { padding: 3.5rem 0 3rem; }
          .rw-head { padding: 0 1.5rem 1.5rem; }
          .rw-strip {
            --rw-card: min(84vw, 480px);
            gap: 1rem;
            padding-block: 0.5rem 1rem;
          }
          .rw-foot { padding: 1rem 1.5rem 0; }
          .rw-strip-item {
            height: auto;
            min-height: 74vh;
          }
          .rw-panel {
            height: 100%;
            grid-template-columns: 1fr;
            grid-template-rows: 1.1fr 0.9fr;
          }
          .rw-panel-visual { padding: 1.5rem; min-height: 40vh; }
          /* Stacked, so the square is driven off the row instead of the column:
             the art row sizes to content and the visual's own aspect-ratio
             resolves against the card's definite width. The 40vh floor has to go
             or it would stretch the square back into a portrait. */
          .rw-panel-square {
            grid-template-columns: 1fr;
            grid-template-rows: auto 1fr;
          }
          .rw-panel-square .rw-panel-visual {
            aspect-ratio: 1;
            min-height: 0;
          }
          /* Every strip item stretches to the tallest one, and a square is
             shorter than the 1.1fr of 74vh the other panels give their art — so
             the copy row inherits the slack. Centred copy would then float in
             the middle of the card with a dead band above it; top-aligned, the
             text sits right under the art and the slack collects at the foot. */
          .rw-panel-square .rw-panel-copy { justify-content: flex-start; }
          .rw-panel-copy {
            padding: 1.75rem;
            background: linear-gradient(180deg, transparent, rgba(0,0,0,0.35) 20%);
          }
          .rw-panel-index { font-size: 2rem; top: 1rem; left: 1.25rem; }
          /* Square key art covers the mobile half without losing the device,
             so it fills here too and the RGB-split tick stays in play. */
          /* Copy sits below the art here, so the still panel's edge fade
             turns vertical too. */
          .rw-still-vig {
            background:
              radial-gradient(120% 110% at 50% 45%, transparent 62%, rgba(0,0,0,0.5) 100%),
              linear-gradient(180deg, transparent 72%, rgba(0,0,0,0.5) 100%);
          }
          /* Copy sits below the art here, so the edge fade turns vertical. */
          .rw-crt-vig {
            background:
              radial-gradient(120% 110% at 50% 45%, transparent 62%, rgba(2,4,14,0.55) 100%),
              linear-gradient(180deg, transparent 72%, rgba(2,4,14,0.55) 100%);
          }
        }

        /* Off-strip / off-screen panels: every ambience layer holds its frame.
           (See the IntersectionObserver above.) */
        .rw-strip-item.rw-idle .rw-marble,
        .rw-strip-item.rw-idle .rw-marble-flow,
        .rw-strip-item.rw-idle .rw-brick-bg,
        .rw-strip-item.rw-idle .rw-land-bg,
        .rw-strip-item.rw-idle .rw-float-device,
        .rw-strip-item.rw-idle .rw-still-art,
        .rw-strip-item.rw-idle .rw-crt-ghost {
          animation-play-state: paused;
        }

        /* Reduced motion: kill all decorative animation + smooth scroll. */
        @media (prefers-reduced-motion: reduce) {
          .rw-marble,
          .rw-marble-flow,
          .rw-brick-bg,
          .rw-land-bg,
          .rw-float-device { animation: none !important; }
          .rw-still-art { animation: none !important; transform: none !important; }
          /* Scanlines stay (they're static texture); the glitch tick does not. */
          .rw-crt-ghost { animation: none !important; opacity: 0 !important; }
          .rw-progress-bar { transition: none !important; }
        }
      `})]})}export{Z as default};
